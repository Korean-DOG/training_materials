package notes

import (
	"testing"
)

/* type Fruit struct {
	color string
}

func (f *Fruit) getColor() (string, error) {
	if f.color != nil{
		return f.color, nil
	}else{
		return "black", errors.New("Must be re-defined")
	}

type iFruit interface{
	getColor()
}

type Apple struct {
	Fruit
}

func (f *Fruit) init() {
	a.color="Red"; return "Red", nil
}

type Banan struct {
	Figure
}

func (c *Banan) Init() (string, error) {
	return "yellow", nil
} */

func TestInheritance3(t *testing.T) {
	/* var apple = Apple{}

	fmt.Println("Perimeter of Triangle = " + fmt.Sprintf("%v", trPerimeter))

	var c = Circle{}
	c.sides = []float32{1.0}
	c_perimeter, err := c.getPerimeter()
	if err != nil {
		fmt.Println("Error with circle")
		//fmt.Errorf(err)
	}
	fmt.Println("Perimeter of Circle = " + fmt.Sprintf("%v", c_perimeter)) */

}
